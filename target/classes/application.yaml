spring:
  application:
    name: virtual-store
  profiles:
    active: dev
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: gabriel
    password:
  h2:
    console:
      enabled: true
      path: /h2

server:
  port: 2405